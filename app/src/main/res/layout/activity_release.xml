<?xml version="1.0" encoding="utf-8"?>
<com.gw.swipeback.SwipeBackLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/swipeBackLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:directionMode="top"
    android:fitsSystemWindows="true"
    app:maskAlpha="0"
    app:swipeBackFactor="0.1">
<androidx.coordinatorlayout.widget.CoordinatorLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <LinearLayout
        android:background="@color/fox_background"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        app:layout_anchorGravity="bottom">

        <Button
            android:id="@+id/installButton2"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginHorizontal="16dp"
            android:layout_marginVertical="12dp"
            android:text="@string/rel_installthis"
            android:textColor="@color/fox_background"
            app:icon="@drawable/ic_round_save_alt_24"
            app:iconGravity="textStart"
            app:iconPadding="12dp"
            app:iconTint="@color/fox_background" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imageView4"
            android:layout_width="match_parent"
            android:layout_height="10dp"
            app:srcCompat="@drawable/rounded_top" />

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/appToolbar"
            style="@style/ToolbarStyle"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            app:titleTextAppearance="@style/Toolbar.TitleText" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:orientation="vertical">

            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardCornerRadius="0dp"
                app:cardElevation="6dp">

                <com.ogaclejapan.smarttablayout.SmartTabLayout
                    android:id="@+id/viewpagertab"
                    style="@style/TabsTransparent"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:background="@color/fox_title_solid_bg"
                    android:clipToPadding="false"
                    android:defaultFocusHighlightEnabled="false"
                    android:paddingStart="54dp"
                    android:paddingEnd="8dp"
                    android:theme="@style/TabsTransparent"
                    app:stl_defaultTabTextColor="@color/white"
                    app:stl_defaultTabTextHorizontalPadding="18dp"
                    app:stl_dividerColor="@android:color/transparent"
                    app:stl_dividerThickness="0dp"
                    app:stl_indicatorColor="@color/fox_card"
                    app:stl_indicatorCornerRadius="18dp"
                    app:stl_indicatorGravity="center"
                    app:stl_indicatorInterpolation="linear"
                    app:stl_indicatorThickness="36dp"
                    app:stl_underlineColor="@android:color/transparent"
                    app:stl_underlineThickness="0dp" />
            </androidx.cardview.widget.CardView>
        </LinearLayout>

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/viewpager"
            android:background="@color/fox_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="64dp" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/loadingLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/fox_status_solid_bg"
        android:orientation="vertical"
        android:weightSum="5">

        <ProgressBar
            android:id="@+id/spinner"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="center" />
    </FrameLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>

</com.gw.swipeback.SwipeBackLayout >